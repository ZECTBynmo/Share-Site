//////////////////////////////////////////////////////////////////////////
// Constructor
function SearchWidget() {
	
	// Turn the html element into a jquery accordion
	$('#userQueueHolder').collapsiblePanel({
	});
}; // end SearchWidget.SearchWidget()